# V17.7 iPad Calendar Navigation Alignment

- Preserves the V16 row-based calendar toolbar on iPad.
- Prevents the legacy `ipad-real-width-fix` class from placing toolbar rows into narrow grid columns.
- Keeps the full date visible and separates previous / Today / next controls.
- Keeps the quick-add button in its own grid column.
- Layout-only change; calendar data, drag logic, Firebase sync, and navigation handlers are unchanged.
